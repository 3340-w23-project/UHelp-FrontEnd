import React from "react";
import Form from "../../../components/Auth/Form";

export const metadata = {
  title: "Sign In",
};

function SignIn() {
  return <Form signInMode />;
}

export default SignIn;
